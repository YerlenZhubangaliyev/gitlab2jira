define(['./base'], function (Base) {
    'use strict';

    return Base.extend({
        defaults: {
            "body":    "",
            "author":  "",
            "created": ""
        }
    });
});